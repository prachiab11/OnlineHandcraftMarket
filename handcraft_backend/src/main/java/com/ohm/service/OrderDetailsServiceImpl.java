package com.ohm.service;

/*import java.util.Iterator;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.context.properties.source.IterableConfigurationPropertySource;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import com.ohm.pojos.OrderDetails;
import com.ohm.repository.IOrderDetailsRepository;

@Service
@Transactional
public class OrderDetailsServiceImpl implements IOrderDetailsService {
	
	@Autowired
	private IOrderDetailsRepository orderDetailsRepo;

	@Override
	public OrderDetails saveOrUpdateAOrderDetails(OrderDetails order) {
		return orderDetailsRepo.save(order);
	}

	@Override
	public OrderDetails findByOrderDetailsId(Long orderDetailsId) {
		
		return orderDetailsRepo.findByOrderDetailsId(orderDetailsId);
	}

	@Override
	public Iterable<OrderDetails> findAll() {
		return orderDetailsRepo.findAll();
	}

	
	
}*/
